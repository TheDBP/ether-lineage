From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: TheDBP <TheDBP@users.noreply.github.com>
Date: Tue, 15 Sep 2026 22:05:38 -0400
Subject: [PATCH] ether: 360 dpi

The Robin's 5.2-inch 1080p panel is 424 dpi; the upstream 480 renders everything a size too
large on a screen this small. 360 gives the 5.2-inch panel enough breathing room for dense
layouts while staying legible; the Display Size ladder SettingsLib builds from
display_density_min_scale (70%) and _min_scale_interval (9%) recomputes around it as
252 / 288 / 324 / 360 and upwards, so anyone who finds it tight can move it in Settings.

TARGET_RECOVERY_DENSITY is pinned because recovery derives its asset bucket from
TARGET_SCREEN_DENSITY, and build/make/core/Makefile splits xxhdpi at >= 400. Without the
pin, 360 would silently drop recovery to xhdpi artwork; the panel itself has not changed.
---
 BoardConfig.mk | 3 ++-
 1 file changed, 2 insertions(+), 1 deletion(-)

diff --git a/BoardConfig.mk b/BoardConfig.mk
index aac9a3a..ac5cece 100644
--- a/BoardConfig.mk
+++ b/BoardConfig.mk
@@ -81,7 +81,8 @@ TARGET_VNDK_USE_CORE_VARIANT := true
 WITH_DEXPREOPT_DEBUG_INFO := false
 
 # Display
-TARGET_SCREEN_DENSITY := 480
+TARGET_SCREEN_DENSITY := 360
+TARGET_RECOVERY_DENSITY := xxhdpi
 TARGET_USES_ION := true
 
 # Encryption
